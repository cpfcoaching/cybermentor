# ─────────────────────────────────────────────────────────────────────────────
# CyberMentor — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Core agent framework (includes Gemini 3.5 access)
google-antigravity>=0.1.0

# Google AI SDK — Veo (video), Lyria (music), Gemma (fast inference)
# Used for bonus Google AI model integrations
google-genai>=0.8.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Google Cloud
google-cloud-firestore>=2.19.0
google-auth>=2.35.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0

# Resume document parsers & generators (PDF, DOCX)
pypdf>=5.0.0
python-docx>=1.1.0
reportlab>=4.0.0

# Zero-Cost Neural Voice Engine (Self-Hosted on Cloud Run)
edge-tts>=6.1.0
